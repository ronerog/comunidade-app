package com.comunidade.catalog.api.dto;

import java.time.LocalDateTime;
import java.util.List;
import java.util.UUID;

// TODO Fase 1: DTO de saída para a vitrine.
public record ServiceResponse(
        // UUID id,
        // String title,
        // String description,
        // String availability,
        // String status,
        // String categoryName,
        // UUID providerId,
        // String providerName,           // join lógico (vem do JWT? do user.registered cache?)
        // List<String> photoUrls,
        // LocalDateTime createdAt
) {
}
